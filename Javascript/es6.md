# 📘 ES6 (ECMAScript 2015) 핵심 문법 정리

---

# 1️⃣ ES6란 무엇인가?

## ✅ 정의

ES6는 **ECMAScript 2015 (ES2015)** 라고도 불리며,  
자바스크립트의 6번째 공식 표준 명세입니다.

자바스크립트의 문법과 기능을 대폭 개선한 중요한 버전으로,  
현대 자바스크립트의 기반이 되는 핵심 업데이트입니다.

---

## ✅ 왜 중요한가?

ES6 이전의 자바스크립트는:

- 변수 스코프 문제 (`var`)
- 복잡한 비동기 처리
- 모듈 시스템 부재
- 반복되는 코드 작성

등의 불편함이 있었습니다.

ES6는 이를 개선하며:

- 가독성 향상
- 유지보수성 개선
- 모듈화 지원
- 함수형 프로그래밍 스타일 강화

를 가능하게 했습니다.

---

# 2️⃣ 주요 문법 정리

---

## 1️⃣ Promise

### ✅ 개념

- 비동기 작업의 **결과를 나타내는 객체**
- 상태:
  - `pending` (대기)
  - `fulfilled` (성공)
  - `rejected` (실패)

### ✅ 기본 사용

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('완료');
  }, 1000);
});

promise.then((result) => console.log(result)).catch((error) => console.error(error));
```

### ✅ async / await

```js
const fetchData = async () => {
  const response = await fetch('/api');
  const data = await response.json();
  return data;
};
```

---

## 2️⃣ Template Literal (`)

### ✅ 특징

- `${}`를 이용한 변수 삽입
- 여러 줄 문자열 지원

```js
const name = '승규';
const message = `안녕하세요 ${name}님`;
```

```js
const text = `
여러 줄
문자열 작성 가능
`;
```

---

## 3️⃣ Spread Operator (`...`)

### ✅ 개념

- 배열이나 객체를 펼쳐서 복사하거나 병합할 때 사용

### ✅ 배열

```js
const arr1 = [1, 2];
const arr2 = [...arr1, 3, 4];
```

### ✅ 객체

```js
const obj1 = { a: 1 };
const obj2 = { ...obj1, b: 2 };
```

### ✅ 함수 인자

```js
const numbers = [1, 2, 3];
Math.max(...numbers);
```

---

## 4️⃣ Destructuring (비구조화 할당)

### ✅ 개념

- 배열이나 객체의 값을 쉽게 꺼내는 문법

### ✅ 배열

```js
const arr = [10, 20];
const [a, b] = arr;
```

### ✅ 객체

```js
const user = { name: '승규', age: 20 };
const { name, age } = user;
```

### ✅ 기본값 설정

```js
const { job = '개발자' } = user;
```

### ✅ 함수 파라미터에서 사용

```js
function Component({ title, onClick }) {
  console.log(title);
}
```

---

## 5️⃣ let / const

### ✅ let

- 재할당 가능
- 블록 스코프

```js
let count = 1;
count = 2;
```

### ✅ const

- 재할당 불가
- 블록 스코프

```js
const name = '승규';
```

⚠️ 객체 내부 값은 변경 가능

```js
const obj = { a: 1 };
obj.a = 2; // 가능
```

---

## 6️⃣ Arrow Function (화살표 함수)

### ✅ 개념

- 기존 `function`을 더 간결하게 표현하는 문법
- `this`가 **자신이 선언된 스코프의 this를 유지 (Lexical this)**

---

### ✅ 기본 문법

```js
// 기존 함수
function add(a, b) {
  return a + b;
}

// 화살표 함수
const add = (a, b) => {
  return a + b;
};
```

---

### ✅ 한 줄 표현 (return 생략 가능)

```js
const add = (a, b) => a + b;
```

---

### ✅ 매개변수 하나일 때

```js
const square = (x) => x * x;
```

---

### ✅ 객체 반환 시 주의

```js
// 소괄호로 감싸야 함
const getUser = () => ({ name: '승규', age: 20 });
```

---

### ✅ this 차이 (중요)

```js
function Test() {
  this.value = 10;

  setTimeout(() => {
    console.log(this.value); // 10 (자기 스코프 유지)
  }, 1000);
}
```

React에서 콜백 함수에 많이 사용됨.

---

## 7️⃣ Rest Parameter (`...`)

### ✅ 개념

- 여러 개의 인자를 **배열로 묶어 받는 문법**

---

### ✅ 기본 사용

```js
const sum = (...numbers) => {
  return numbers.reduce((acc, cur) => acc + cur, 0);
};

sum(1, 2, 3, 4); // 10
```

`numbers`는 배열이 됨.

---

### ✅ 일부 매개변수 + Rest

```js
const print = (first, ...others) => {
  console.log(first); // 첫 번째 값
  console.log(others); // 나머지 배열
};

print(1, 2, 3, 4);
```

---

### 🔥 Spread vs Rest 차이

| 구분   | 역할        |
| ------ | ----------- |
| Spread | 펼쳐서 사용 |
| Rest   | 모아서 받기 |

---

## 8️⃣ Default Parameter (기본값 매개변수)

### ✅ 개념

- 함수 매개변수에 기본값을 설정할 수 있음

---

### ✅ 기본 사용

```js
const greet = (name = '익명') => {
  console.log(`안녕하세요 ${name}`);
};

greet(); // 안녕하세요 익명
greet('승규'); // 안녕하세요 승규
```

---

### ✅ 여러 개 설정

```js
const multiply = (a = 1, b = 1) => a * b;

multiply(); // 1
multiply(2, 3); // 6
```

---

### ✅ 객체 기본값과 함께 사용

```js
const createUser = ({ name = '이름없음', age = 0 } = {}) => {
  return { name, age };
};

createUser();
createUser({ name: '승규' });
```

---

# 📦 지금까지 정리된 ES6 핵심

- Promise
- Template Literal
- Spread Operator
- Destructuring
- let / const
- Arrow Function
- Rest Parameter
- Default Parameter
